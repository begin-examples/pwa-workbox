{
  "name": "pwa-workbox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p src/shared/build && echo \"// autogenerated build time for caching\nexports.BUILD_TIME = $(date '+%s')\"> src/shared/build/build-time.js ",
    "start": "npm run build && npx sandbox"
  },
  "author": "Ryan Bethel",
  "license": "ISC",
  "devDependencies": {
    "@architect/eslint-config": "^2.0.1",
    "@architect/sandbox": "^4.4.2",
    "eslint": "^8.3.0"
  },
  "dependencies": {
    "@architect/functions": "^4.1.1"
  },
  "eslintConfig": {
    "extends": "@architect/eslint-config"
  }
}